# Baked into the bundle at build time, so canonical links and OG image urls point at the real site
# instead of whatever `.env.local` says. Mode-specific files win over `.env.local`.
VITE_SITE_URL=https://freshgiammi.dev
